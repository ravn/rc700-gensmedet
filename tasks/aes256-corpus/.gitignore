# Build artifacts — regenerated by `make` and the flag sweep scripts.
*.o
*.bin
*.elf
*.ram
*.tstates
*.end_addr
*.map
*.asm
*.lis
*.buildlog
*.c.asm

# Generated linker / startup files (Makefile regenerates these).
reset_clang.s
clang.ld

# Per-compiler driver outputs from zcc (no extension).
zsdcc
clang

# Sweep scratch directories.
sweep/
sweep_*/

# Reference files we DO keep (overrides above globs):
!DEMO.COM
!aes256-original.zip
